@model IEnumerable<ManutencaoRegistrosComprasGado.Models.CompraGadoItem>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompraGado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Animal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompraGado.CompraGadoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Animal.Descricao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CompraGadoItemId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CompraGadoItemId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CompraGadoItemId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
