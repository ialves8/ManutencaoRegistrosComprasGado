@model IEnumerable<ManutencaoRegistrosComprasGado.Models.CompraGado>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataEntrega)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pecuarista)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataEntrega)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pecuarista.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CompraGadoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CompraGadoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CompraGadoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
