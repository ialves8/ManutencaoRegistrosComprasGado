@model IEnumerable<ManutencaoRegistrosComprasGado.Models.Pecuarista>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PecuaristaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PecuaristaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PecuaristaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
